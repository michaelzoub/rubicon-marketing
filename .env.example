# Rubicon gateway (server-side calls / SSR)
RUBICON_API_URL=https://api.rubicon.dev

# Rubicon gateway (browser-side calls from the dashboard)
NEXT_PUBLIC_RUBICON_API_URL=https://api.rubicon.dev

# Privy authentication
NEXT_PUBLIC_PRIVY_APP_ID=your_privy_app_id
NEXT_PUBLIC_PRIVY_CLIENT_ID=your_privy_app_client_id
PRIVY_APP_SECRET=your_privy_app_secret_for_server_verification
